import 'package:flutter/material.dart';

class SedesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("📍 Sedes", style: TextStyle(fontSize: 24)));
  }
}